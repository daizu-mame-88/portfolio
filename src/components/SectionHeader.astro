---
interface Props {
    title: string;
    subtitle?: string;
}

const { title, subtitle } = Astro.props;
---

<div class="mb-12 md:mb-16">
    <h2 class="heading-section mb-3">{title}</h2>
    {subtitle && <p class="heading-sub max-w-xl">{subtitle}</p>}
</div>

